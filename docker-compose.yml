services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: pentraai-backend
    env_file:
      - ./backend/.env
    ports:
      - "8000:8000"
    restart: unless-stopped
